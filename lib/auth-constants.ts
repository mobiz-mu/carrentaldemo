// ============================================================
// Edge-safe auth constants (no Node-only imports).
// Safe to import from middleware (Edge runtime).
// ============================================================

export const ADMIN_COOKIE = "mobiz_admin";
export const ADMIN_SESSION_VALUE = "ok"; // demo session marker
